#!/usr/bin/env python

"""Django's App config definition for Satapp."""

from django.apps import AppConfig


class SatappConfig(AppConfig):
    """Satapp Config."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'satapp'
